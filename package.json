{
  "name": "remedy-search",
  "version": "1.0.0",
  "description": "EPUB-based traditional remedy search with Amazon affiliate links",
  "main": "api/index.py",
  "scripts": {
    "dev": "python -m uvicorn api.index:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Build complete'",
    "start": "python -m uvicorn api.index:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "python": "3.11"
  }
}